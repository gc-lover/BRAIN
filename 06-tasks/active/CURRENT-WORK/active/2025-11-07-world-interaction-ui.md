**Статус:** in_progress  
**Приоритет:** high  
**Дата создания:** 2025-11-07 16:39  
**Дата завершения:** —  
**Связанные документы:** `05-technical/ui/main-game/ui-hud-core.md`, `05-technical/ui/main-game/ui-features.md`, `02-gameplay/world/world-state/player-impact-mechanics.md`, `05-technical/global-state/global-state-operations.md`

---

# Интерфейсы мира и связанные игровые механики

## 1. Цель обсуждения
- Сформировать единый каркас интерфейсов, которые дают игроку контроль над состоянием мира.
- Описать игровые механики, напрямую завязанные на эти интерфейсы.
- Зафиксировать функции поддержки мира, требующиеся для стабильной работы live-сервиса.
- Подготовить основу для API-задач `world-service (8086)` и фронтенд модулей `modules/world`, `modules/economy`, `modules/social`.

## 2. Ключевые интерфейсы

### 2.1 Панель состояния мира (World Pulse)
- Расположение: главный HUD, отдельный виджет в правом верхнем углу.
- Содержимое: глобальный модификатор мира, активные события, индикатор стабильности, кнопка перехода к детальному дашборду.
- Обновление: WebSocket поток от `world-service` каждые 5 секунд, fallback GET `/api/v1/world/state`.
- Визуальные компоненты: `shared/ui/WorldStatusCard`, `shared/ui/LiveEventTicker`.

### 2.2 Дашборд мировых событий
- Отдельный экран, доступ через World Pulse и меню `📊 Stats`.
- Разделы: глобальные события (PvE), региональные кризисы, сезонные активности, статус фракционных войн.
- Функциональность: фильтрация по региону/фракции, подписка на события, просмотр истории воздействия игроков.
- API: `GET /api/v1/world/events`, `GET /api/v1/world/history?regionId=`.
- UI модуль: `modules/world/events-dashboard`, использует `shared/ui/CyberpunkTable`, графики `shared/ui/HoloChart`.

### 2.3 Карта влияния (Influence Map)
- Расположение: вкладка карты (`🗺️ Map`) с дополнительным слоем.
- Механика: отображение уровней контроля территорий, трафика игроков, активности NPC.
- Интерактив: включение тепловой карты (heatmap), просмотр информации о контролирующей фракции, запуск миссий напрямую.
- API: `GET /api/v1/world/territories`, `GET /api/v1/world/territories/{id}/metrics`, стрим `ws://world-service/territories/stream`.
- Фронтенд: `modules/world/map`, компонент `shared/ui/CyberpunkHeatmap`, интеграция с `auth` для проверки прав.

### 2.4 Пульт управления гильдии (Guild Operations)
- Раздел социального модуля `modules/social/guild-hub`.
- Таблица вкладов игроков, планировщик обороны, каскад разрешений.
- Триггеры: массовые приказы на поддержку региона, распределение ресурсов, запуск гильдейских эвентов.
- API: `POST /api/v1/world/orders`, `GET /api/v1/world/orders/pending`, `POST /api/v1/world/orders/{id}/resolve`.

### 2.5 Экономический терминал (Market Stabilizer)
- Расположение: `modules/economy/market-terminal`, вкладка «Стабилизация».
- Показывает текущие цены, прогнозы, рекомендации по действиям гильдий.
- Позволяет запускать механизмы «ценового демпфера» (контракт с городом) и «стратегического резерва».
- API: `GET /api/v1/economy/market/overview`, `POST /api/v1/economy/market/intervention` (делегируется `economy-service (8085)` с подтверждением от `world-service`).

### 2.6 Панель романтических и социальных эффектов (Social Resonance)
- Расположение: `modules/social/resonance`.
- Отображает, как отношения и репутация меняют доступность событий и модификаторы мира.
- Механики: отображение перк-слотов, влияющих на глобальный индекс настроений.
- API: `GET /api/v1/social/resonance`, `GET /api/v1/world/modifiers/social`.

## 3. Связанные игровые механики

### 3.1 Система динамических событий
- Категории: глобальные (Night City blackout), региональные (корпоративные рейды), локальные (NPC цепочки).
- Триггеры: прогресс квестов, уровень хаоса в регионе, решения игроков через Guild Operations.
- Влияние: изменение доступности миссий, временные бонусы/штрафы, запуск эвакуирования NPC.
- Контроль: интерфейсы World Pulse и Events Dashboard показывают состояние и таймеры.

### 3.2 Управление территориями и властью
- Механика контроля: суммирование вкладов игроков (PvP, PvE, экономические контракты).
- Пороговые значения: 25%, 50%, 75% контроля дают разные уровни бонусов.
- Взаимодействие UI: Influence Map показывает цветовую диаграмму, Guild Operations выдает приказы.
- Обновления: `world-service` агрегирует вклад каждый час, пересчитывает контроль, пушит обновления через WebSocket.

### 3.3 Экономические стабилизационные механизмы
- Игроки инициируют временные меры через Market Stabilizer.
- Экономический эффект: временное замораживание цен, выпуск государственных облигаций, штрафы за спекуляции.
- Мировой эффект: Global State фиксирует показатель стабильности, отображается в World Pulse.

### 3.4 Социальный резонанс и настроение населения
- Репутация гильдий и романтические связи увеличивают или уменьшают «индекс доверия».
- UI отображает шкалу настроения, доступ к эксклюзивным событиям, механики anti-riot при низком индексе.
- Влияние на мир: низкий индекс открывает кризисные события, высокий снижает хаос и стоимость страховок.

### 3.5 Режим реагирования на катастрофы
- При глобальных событиях UI включает режим «Crisis Control»: красная тема, приоритетные уведомления.
- Игровые механики: обязательные эвакуации, усиленные патрули NPC, штрафы за PvP.
- Поддерживающие функции: автоматический запуск скриптов `world-service` для перераспределения ресурсов и синхронизации с `economy-service`.

## 4. Функции поддержки мира

### 4.1 Планировщики и фоновые процессы
- `world-service`:
  - Scheduler `WorldTickJob` каждые 60 секунд обновляет метрики регионов.
  - `EventSequencer` управляет очередями событий, гарантирует causal order.
  - `CrisisMitigationJob` активирует защитные сценарии при падении индекса стабильности ниже 30%.
- `economy-service`:
  - `PriceNormalizationJob` синхронизирует результаты стабилизационных мер.
  - `ResourceReserveSync` проверяет наличие ресурсов для государственных контрактов.

### 4.2 Мониторинг и алертинг
- Метрики Prometheus: `world.state.stability`, `world.events.active`, `world.sync.lag`.
- Alert правила: лаг синхронизации > 5 сек, количество активных кризисов > 3, ошибка обработчика заказов > 0.05%.
- UI админ-панели (`05-technical/ui/admin-panel/part2-analytics-controls.md`) получает aggregated feed через `GET /api/v1/admin/world/metrics`.

### 4.3 Инструменты модерации и GM
- Админ-интерфейс позволяет вручную корректировать состояние мира, запускать тестовые эвенты.
- API: `POST /api/v1/world/admin/override`, `POST /api/v1/world/admin/event` с RBAC контролем.
- Вкладка `GM Tools` в админ-панели подключается к `modules/world/admin`.

### 4.4 Тестирование и симуляции
- `world-service` включает `ScenarioSimulator` для прогонов кризисов (nightly build).
- Тестовые данные выводятся в `readiness-tracker` для оценки устойчивости.
- UI режим «Spectator» позволяет QA наблюдать за симуляцией через Events Dashboard.

## 5. Интеграция фронтенд модулей
- `modules/world` отвечает за World Pulse, Events Dashboard, Influence Map.
- `modules/economy` предоставляет Market Stabilizer и интеграцию с торговыми интерфейсами.
- `modules/social` реализует Guild Operations и Social Resonance панели.
- Общие компоненты: `shared/ui/CyberpunkMap`, `shared/ui/StatPill`, `shared/ui/StatusPulse`.
- State management: `@necp/state/world` slice, синхронизация через RTK Query с `world-service`.

## 6. Следующие шаги
1. Согласовать требования интерфейсов с UI/UX командой и обновить `05-technical/ui/main-game/ui-system.md` (ответственный: Front Manager, срок: 2025-11-08).
2. Подготовить детальную спецификацию API для World Pulse и Events Dashboard (ответственный: Brain → API Task Creator, срок: 2025-11-09).
3. Определить SLA для фоновых задач `world-service` и `economy-service`, дополнить `global-state-management.md` (ответственный: Backend Manager, срок: 2025-11-10).
4. Спланировать user testing сценарии для Crisis Control режима (ответственный: QA Lead, срок: 2025-11-11).

## 7. Открытые вопросы
1. Требуется ли игрокам возможность временно отключать World Pulse для снижения информационной нагрузки? Нужна UX оценка.
2. Нужен ли мобильный companion интерфейс для гильдий? Решить с продуктовой командой.
3. Какой формат реплея мировых событий должен быть доступен игроку в UI (видео/текст/граф)?

---

**Статус обновлен:** 2025-11-07 16:39

