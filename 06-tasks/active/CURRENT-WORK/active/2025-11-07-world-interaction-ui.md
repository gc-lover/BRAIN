---
**api-readiness:** ready  
**api-readiness-check-date:** 2025-11-07 18:05  
**api-readiness-notes:** UX мира обновлён Specter overlay и монитором городских беспорядков; World Pulse, Events Dashboard, Influence Map, Guild Ops и Crisis Hub готовы к API постановке (`world-service`, `economy-service`, `social-service`).
---

**Статус:** approved  
**Версия:** 1.0.0  
**Дата создания:** 2025-11-07 16:39  
**Дата обновления:** 2025-11-07 18:05  
**Приоритет:** high  
**Связанные документы:** `05-technical/ui/main-game/ui-hud-core.md`, `05-technical/ui/main-game/ui-features.md`, `02-gameplay/world/world-state/player-impact-mechanics.md`, `05-technical/global-state/global-state-operations.md`

---

# Интерфейсы мира и связанные игровые механики

## 1. Цель обсуждения
- Сформировать единый каркас интерфейсов, которые дают игроку контроль над состоянием мира.
- Описать игровые механики, напрямую завязанные на эти интерфейсы.
- Зафиксировать функции поддержки мира, требующиеся для стабильной работы live-сервиса.
- Подготовить основу для API-задач `world-service (8086)` и фронтенд модулей `modules/world`, `modules/economy`, `modules/social`.

## 2. Ключевые интерфейсы

### 2.1 Панель состояния мира (World Pulse)
- Расположение: главный HUD, отдельный виджет в правом верхнем углу.
- Содержимое: глобальный модификатор мира, активные события, индикатор стабильности, кнопка перехода к детальному дашборду.
- Обновление: WebSocket поток от `world-service` каждые 5 секунд, fallback GET `/api/v1/world/state`.
- Визуальные компоненты: `shared/ui/WorldStatusCard`, `shared/ui/LiveEventTicker`.
- Режимы отображения: `calm`, `alert`, `crisis` в зависимости от `alertLevel`.
- Состояния UI: `loading` (скелетон), `connected`, `degraded` (потеря realtime), `error`.
- Датамодель: `{ modifierType, modifierValue, stabilityIndex, activeEvents[], alertLevel, updatedAt }`.
- Локальная деградация: RTK Query хранит snapshot 90 секунд, затем показывает предупреждение и инициирует refetch.
- CTA: «Открыть дашборд», «Прогноз», «Кризисный центр» (появляется при `alertLevel=crisis`).

### 2.2 Дашборд мировых событий
- Отдельный экран, доступ через World Pulse и меню `📊 Stats`.
- Разделы: глобальные события (PvE), региональные кризисы, сезонные активности, статус фракционных войн.
- Функциональность: фильтрация по региону/фракции, подписка на события, просмотр истории воздействия игроков.
- API: `GET /api/v1/world/events`, `GET /api/v1/world/history?regionId=`.
- UI модуль: `modules/world/events-dashboard`, использует `shared/ui/CyberpunkTable`, `shared/ui/HoloChart`.
- Пагинация: серверная, шаг 20 записей, поддержка бесконечной подгрузки.
- Состояния: `loading`, `empty`, `hasData`, `error`, `offline-cache`.
- WebSocket `ws://world-service/events/live` присылает `WORLD_EVENT_UPDATE`, diff применяется по `eventId`.
- UX flow: фильтр → список → карточка → вкладка «История влияния».
- Экспорт CSV для ролей `guild-officer` и выше.

### 2.3 Карта влияния (Influence Map)
- Расположение: вкладка карты (`🗺️ Map`) с дополнительным слоем.
- Механика: отображение уровней контроля территорий, трафика игроков, активности NPC.
- Интерактив: включение тепловой карты, просмотр информации о контролирующей фракции, запуск миссий напрямую.
- API: `GET /api/v1/world/territories`, `GET /api/v1/world/territories/{id}/metrics`, стрим `ws://world-service/territories/stream`.
- Фронтенд: `modules/world/map`, компонент `shared/ui/CyberpunkHeatmap`, интеграция с `auth` для проверки прав.
- Слои: `control`, `population`, `economy`, `security`, `events`.
- Масштабирование: tile-based, тайлы 512 px, lazy loading, клиентский кэш 5 минут.
- Роли: `player` (базовый слой), `guild-officer` (прогнозы), `gm` (debug).
- Ховер показывает бонусы, pending events, вклад гильдий.
- Стейт-машина UI: `idle` → `selecting` → `details` → `action-confirm`.

### 2.4 Пульт управления гильдии (Guild Operations)
- Раздел социального модуля `modules/social/guild-hub`.
- Таблица вкладов игроков, планировщик обороны, каскад разрешений.
- Триггеры: массовые приказы на поддержку региона, распределение ресурсов, запуск гильдейских эвентов.
- API: `POST /api/v1/world/orders`, `GET /api/v1/world/orders/pending`, `POST /api/v1/world/orders/{id}/resolve`.
- Роли: `leader`, `strategist`, `logistics`, `support`.
- Workflow приказа: `draft` → `review` → `active` → `completed/failed`.
- Компонент `GuildOrderTimeline` отображает статусы и дедлайны.
- Нотификации: канал `guild-events`, мобильный companion (roadmap Q1 2026).
- Подтверждения требуют двух ролей (4-eyes principle).

### 2.5 Экономический терминал (Market Stabilizer)
- Расположение: `modules/economy/market-terminal`, вкладка «Стабилизация».
- Показывает текущие цены, прогнозы, рекомендации.
- Позволяет запускать механизмы «ценового демпфера» и «стратегического резерва».
- API: `GET /api/v1/economy/market/overview`, `POST /api/v1/economy/market/intervention` (с подтверждением world-service).
- Аналитика: график «цена против интервенций», прогноз на 24 часа, рисковые индикаторы.
- Подтверждение: MFA (код + лидер), смарт-контракт world-service проверяет лимиты.
- Ответы содержат `interventionId`, статус отслеживается через `GET /api/v1/economy/market/intervention/{id}`.
- Состояния UI: `idle`, `configuring`, `confirming`, `executing`, `completed`, `failed`.
- Ограничения: максимум две активные интервенции на регион, cooldown 8 часов.

### 2.6 Панель романтических и социальных эффектов (Social Resonance)
- Расположение: `modules/social/resonance`.
- Отображает влияние отношений и репутации на доступность событий и модификаторы.
- Механики: перк-слоты, влияющие на глобальный индекс настроений.
- API: `GET /api/v1/social/resonance`, `GET /api/v1/world/modifiers/social`.
- Состояния настроения: `harmony`, `neutral`, `unrest`.
- Вкладки: «Романтические связи», «Гильдейские альянсы», «Городские кампании».
- Расчёт индекса: репутация 40%, романтика 20%, социальные события 40%.
- Запуск кампаний через `POST /api/v1/social/campaigns`, UI показывает влияние и стоимость.
- Компоненты: `shared/ui/ResonanceMeter`, `shared/ui/RelationshipGrid`.
- Specter overlay отображает панель «Underlink Specter» при `flag.neon.elite == true`, содержит текущие поручения Ghosts и статус курьеров.
- Индикатор `city.unrest.level` связывается с world-service и подсвечивает рост беспорядков, предлагая CTA «Сдержать протесты» при превышении порога.
- Specter overlay показывает панель «Underlink Specter» при флаге `flag.neon.elite == true`, отражает поручения Ghosts и статус курьеров.
- Индикатор `city.unrest.level` визуализирует рост беспорядков, при превышении порога активирует CTA «Сдержать протесты».

### 2.7 Кризисный центр (Crisis Control Hub)
- Активируется при `alertLevel=crisis`.
- Разделы: «Эвакуация», «Милитаризация», «Поддержка экономики».
- Действия: запуск государственных контрактов, назначение safe zones, временная блокировка PvP.
- API: `GET /api/v1/world/crisis/status`, `POST /api/v1/world/crisis/actions`.
- Состояния UI: `monitoring`, `action-selection`, `action-confirmed`, `cooldown`.
- Обязательные подтверждения: world-service + social-service для социальных мер.
- Таймеры действий отслеживаются в `CrisisActionTimeline`, показывают cooldown и эффект.
- Логи действий доступны офицерам для анализа устойчивости (export CSV).
- Интеграция с `modules/economy` для запуска стабилизационных контрактов напрямую.

## 3. Связанные игровые механики

### 3.1 Система динамических событий
- Категории: глобальные (Night City blackout), региональные (корпоративные рейды), локальные (NPC цепочки).
- Триггеры: прогресс квестов, уровень хаоса в регионе, решения игроков через Guild Operations.
- Влияние: изменение доступности миссий, временные бонусы/штрафы, запуск эвакуирования NPC.
- Контроль: интерфейсы World Pulse и Events Dashboard показывают состояние и таймеры.
- Стейт-машина: `scheduled` → `announced` → `active` → `resolution` → `cooldown` → `archived`.
- Таймеры синхронизируются по серверу, допустимый дрейф ±1 сек.
- Балансировка зависит от количества участников и уровня региона, хранится в `event_scaling_rules`.
- Метрики: `events.active.count`, `events.participation.rate`, `events.failures.total`.

### 3.2 Управление территориями и властью
- Механика контроля: суммирование вкладов игроков (PvP, PvE, экономические контракты).
- Пороговые значения: 25%, 50%, 75% контроля дают разные уровни бонусов.
- Взаимодействие UI: Influence Map показывает цветовую диаграмму, Guild Operations выдает приказы.
- Обновления: `world-service` агрегирует вклад каждый час, пересчитывает контроль, пушит обновления через WebSocket.
- Бонусы: `supplyBoost`, `reinforcementSpeed`, `taxReduction` отображаются в World Pulse.
- При падении контроля ниже 10% запускается defense mission и предупреждение в UI.
- Данные хранятся в `world_state.territory.*`, партиционирование по регионам.

### 3.3 Экономические стабилизационные механизмы
- Игроки инициируют временные меры через Market Stabilizer.
- Экономический эффект: временное замораживание цен, выпуск государственных облигаций, штрафы за спекуляции.
- Мировой эффект: Global State фиксирует показатель стабильности, отображается в World Pulse.
- Ограничение: не более 2 активных интервенций на регион, ожидание 8 часов между запуском.
- Успех повышает стабильность на 5-12% и даёт бонус к репутации гильдии.
- Провал снижает стабильность на 10% и активирует штрафы на рынке.
- `DistributedLockService` предотвращает параллельные интервенции.

### 3.4 Социальный резонанс и настроение населения
- Репутация гильдий и романтические связи увеличивают или уменьшают «индекс доверия».
- UI отображает шкалу настроения, доступ к эксклюзивным событиям, механики anti-riot при низком индексе.
- Влияние на мир: низкий индекс открывает кризисные события, высокий снижает хаос и стоимость страховок.
- Индекс доверия пересчитывается nightly, хранится в `world_state.social.trustIndex`.
- Anti-riot сценарий «City Unrest» активируется при индексе <20%.
- Романтические сюжеты добавляют временные модификаторы и VIP-контент.
- Гильдейские кампании влияют на индекс и отображаются в Social Resonance.

### 3.5 Режим реагирования на катастрофы
- При глобальных событиях UI включает режим «Crisis Control»: красная тема, приоритетные уведомления.
- Игровые механики: обязательные эвакуации, усиленные патрули NPC, штрафы за PvP.
- Поддерживающие функции: автоматический запуск скриптов `world-service` для перераспределения ресурсов и синхронизации с `economy-service`.
- Этапы: `preparation`, `response`, `recovery`, каждый со своим набором доступных действий.
- Флаги состояния: `world.crisis.active`, `world.crisis.type`, `world.crisis.mitigationLevel`.
- Отчётность: после `recovery` формируется аналитический отчёт, обновляются стабильность и индекс доверия, игроки получают персональные статистики вклада.
- UI-компоненты: `CrisisBanner`, `EvacuationTracker`, `MitigationGauge`.

## 4. Функции поддержки мира

### 4.1 Планировщики и фоновые процессы
- `world-service`:
  - Scheduler `WorldTickJob` каждые 60 секунд обновляет метрики регионов.
  - `EventSequencer` управляет очередями событий и гарантирует causal order.
  - `CrisisMitigationJob` активирует защитные сценарии при падении стабильности <30%.
- `economy-service`:
  - `PriceNormalizationJob` синхронизирует результаты стабилизаций.
  - `ResourceReserveSync` проверяет ресурсы для государственных контрактов.
- Audit: `world_service_jobs_audit` хранит историю запусков (retention 30 дней).
- Locks: `DistributedLockService` предотвращает дублирование задач на нескольких нодах.
- Health-check: каждая джоба публикует `job.lastRunAt`; UI админ-панели подсвечивает задержки.

### 4.2 Мониторинг и алертинг
- Метрики Prometheus: `world.state.stability`, `world.events.active`, `world.sync.lag`.
- Alert правила: лаг >5 сек, активных кризисов >3, ошибки обработчика >0.05%.
- UI админ-панели получает aggregated feed через `GET /api/v1/admin/world/metrics`.
- Логи: централизованный стек ELK, ключевые поля `eventId`, `regionId`, `severity`.
- SLO: 99.5% uptime World Pulse, доставка критичных уведомлений <2 сек.
- Алерты уходят в Slack/Matrix world-ops, on-call через PagerDuty.

### 4.3 Инструменты модерации и GM
- Админ-интерфейс позволяет вручную корректировать состояние мира, запускать тестовые эвенты.
- API: `POST /api/v1/world/admin/override`, `POST /api/v1/world/admin/event` (RBAC).
- Вкладка `GM Tools` подключается к `modules/world/admin`.
- Действия: установка модификаторов, запуск симуляций, откат состояния, форс-завершение событий.
- Audit trail: `gm_actions_log` с цифровой подписью и описанием причины.
- Sandbox: отдельный shard для тестирования, переключается из UI.

### 4.4 Тестирование и симуляции
- `world-service` включает `ScenarioSimulator` для прогонов кризисов (nightly).
- Тестовые данные отправляются в `readiness-tracker`.
- UI режим «Spectator» позволяет QA наблюдать симуляции через Events Dashboard.
- Сценарии описываются в `05-technical/global-state/scenarios/*.yaml`.
- `SimulatorReport` сохраняется в `05-technical/global-state/` и рассылается в QA канал.
- Nightly pipeline выполняет визуальные снапшоты UI и сравнение с эталоном.

## 5. UX-флоу по ключевым поверхностям

### 5.1 World Pulse
1. Клиент получает snapshot `GET /api/v1/world/state` при загрузке HUD.
2. Подписывается на `ws://world-service/world-state` и обновляет значения каждого 5-секундного тикета.
3. При переходе в `alert` или `crisis` автоматически открывает toast с CTA («Прогноз», «Кризисный центр»).
4. Пользователь переходит в Events Dashboard или Crisis Hub → state slice фиксирует источник перехода для аналитики.

### 5.2 Events Dashboard
1. Игрок открывает экран через `📊 Stats` → триггер `GET /api/v1/world/events?region=`.
2. Применяет фильтры (регион, фракция, категория) → серверная пагинация (20 записей).
3. Подписывается на `WORLD_EVENT_UPDATE`; обновления применяются диффами.
4. При выборе события открывается вкладка «История влияния» (`GET /api/v1/world/history?eventId=`) и предлагается подписка на уведомления.

### 5.3 Influence Map
1. Пользователь активирует режим Influence Map на карте → клиент подгружает слой `control`.
2. При изменении масштаба запрашиваются тайлы 10×10 км (`GET /api/v1/world/territories/{tileId}`).
3. Ховер открывает карточку территории (контролирующая фракция, бонусы, pending events).
4. Если роль ≥ `guild-officer`, доступна кнопка «Отдать приказ» → редирект в Guild Operations с предзаполненной территорией.

### 5.4 Guild Operations
1. Лидер создаёт приказ в статусе `draft`, указывает цель/бюджет → `POST /api/v1/world/orders`.
2. Стратег или logistics подтверждают (`review` → `active`) через `POST /api/v1/world/orders/{id}/resolve`.
3. Таймлайн обновляется в real-time, игроки получают уведомления в гильдейском канале.
4. По завершении система фиксирует KPI приказа и отправляет данные в World Pulse/Influence Map.

### 5.5 Market Stabilizer
1. Гильдия открывает вкладку стабилизации → `GET /api/v1/economy/market/overview` с прогнозами.
2. Настраивает интервенцию (объём, длительность) → предварительный расчёт эффекта.
3. Подтверждение проходит через MFA и смарт-контракт (`POST /api/v1/economy/market/intervention`).
4. Статус отслеживается до `completed/failed`; при сбое игрокам приходит предупреждение в World Pulse.

### 5.6 Social Resonance
1. Игрок открывает вкладку → клиент получает `GET /api/v1/social/resonance` + modifiers.
2. Анализирует вклад фракций/романтических веток, выбирает кампанию.
3. Запускает кампанию (`POST /api/v1/social/campaigns`) → UI показывает эффект и ожидаемый рост индекса.
4. При индексе <20% появляется предупреждение и CTA «Сдержать протесты» (anti-riot).

### 5.7 Crisis Control Hub
1. При `alertLevel=crisis` HUD открывает баннер с переходом в Crisis Hub.
2. Команда выбирает направление (эвакуация/милитаризация/экономика) → действия фиксируются `POST /api/v1/world/crisis/actions`.
3. `CrisisActionTimeline` отслеживает cooldown и результат каждой меры.
4. После `recovery` генерируется отчёт, который доступен лидерам и GM для анализа.

## 6. API и события (сводная таблица)

| Поверхность | Тип | Endpoint / Event | Сервис | Частота / TTL | Примечание |
|-------------|-----|------------------|--------|---------------|------------|
| World Pulse | REST | `GET /api/v1/world/state` | world-service | 60 сек snapshot | fallback при потере WS |
| World Pulse | WS | `ws://world-service/world-state` (`WORLD_STATE_TICK`) | world-service | каждые 5 сек | включает `alertLevel`, `stabilityIndex` |
| Events Dashboard | REST | `GET /api/v1/world/events` | world-service | по запросу | серверная пагинация |
| Events Dashboard | WS | `WORLD_EVENT_UPDATE` | world-service | realtime | дифф полей события |
| Influence Map | REST | `GET /api/v1/world/territories/{tileId}` | world-service | по запросу | tile-based данные |
| Influence Map | WS | `TERRITORY_CONTROL_CHANGED` | world-service | при изменении | включает `factionId`, `delta` |
| Guild Operations | REST | `POST /api/v1/world/orders` | world-service | on-demand | создаёт приказ |
| Guild Operations | REST | `POST /api/v1/world/orders/{id}/resolve` | world-service | переход статуса | применяет решения ролей |
| Market Stabilizer | REST | `POST /api/v1/economy/market/intervention` | economy-service | on-demand | требует MFA + smart contract |
| Market Stabilizer | REST | `GET /api/v1/economy/market/intervention/{id}` | economy-service | каждые 30 сек | polling статуса |
| Social Resonance | REST | `GET /api/v1/social/resonance` | social-service | по запросу | возвращает индекс настроения |
| Social Resonance | REST | `POST /api/v1/social/campaigns` | social-service | on-demand | запускает кампанию |
| Crisis Hub | REST | `POST /api/v1/world/crisis/actions` | world-service | on-demand | требует двойного подтверждения |
| Crisis Hub | WS | `CRISIS_STATE_UPDATE` | world-service | ≤3 сек | обновляет таймеры и уровень смягчения |
| City Unrest Monitor | WS | `CITY_UNREST_UPDATE` | world-service | ≤3 сек | транслирует модификатор `city.unrest.level` для Specter overlay |

| Событие | Источник | Подписчики | Payload (ключевые поля) |
|---------|----------|------------|-------------------------|
| `WORLD_STATE_TICK` | world-service | HUD, World Pulse | `stabilityIndex`, `alertLevel`, `modifier` |
| `WORLD_EVENT_UPDATE` | world-service | Events Dashboard, Notification Center | `eventId`, `state`, `participants`, `timer` |
| `TERRITORY_CONTROL_CHANGED` | world-service | Influence Map, Guild Ops | `territoryId`, `factionId`, `controlPercent` |
| `ORDER_STATUS_CHANGED` | world-service | Guild Ops UI, Notifications | `orderId`, `status`, `approvedBy`, `deadline` |
| `MARKET_INTERVENTION_STATUS` | economy-service | Market Stabilizer, World Pulse | `interventionId`, `status`, `effect` |
| `SOCIAL_INDEX_CHANGED` | social-service | Social Resonance, World Pulse | `trustIndex`, `delta`, `reason` |
| `CRISIS_STATE_UPDATE` | world-service | Crisis Hub, Notifications | `phase`, `actions`, `mitigationLevel`, `eta` |
| `CITY_UNREST_UPDATE` | world-service | Social Resonance, Specter overlay | `unrestLevel`, `trend`, `affectedDistricts` |

## 7. SLA и операционные параметры

- World Pulse: задержка доставки realtime ≤ 2 сек; fallback REST не реже 60 сек; деградация → баннер «данные устарели».
- Events Dashboard: `WORLD_EVENT_UPDATE` ≤ 3 сек; обещанный RPO истории ≤ 5 мин; логи событий хранятся 30 дней.
- Influence Map: пересчёт контроля каждый час; WebSocket обновление теряет connection → автопереподключение 5 попыток.
- Guild Operations: подтверждение приказа ≤ 10 сек; audit trail сохраняется 180 дней; все операции требуют idempotency key.
- Market Stabilizer: интервенция должна получить статус `executing` ≤ 15 сек; при превышении — автроолбек.
- Social Resonance: nightly пересчёт индекса (02:00 UTC); realtime изменения (кампании/романтика) применяются ≤ 30 сек.
- City Unrest Monitor: `CITY_UNREST_UPDATE` распространяется ≤ 3 сек; при уровне >60 активируется CTA Specter и предупреждение в World Pulse.
- Crisis Control Hub: SLA на новые действия ≤ 5 сек; митигирующие отчёты доступны ≤ 2 мин после окончания crisis-фазы.
- Monitoring & Alerts: `world.sync.lag` > 5 сек → PagerDuty P1; `events.active.count` > 50 или `world.state.stability` < 35 → P2.
- QA Simulations: nightly build длится ≤ 45 мин; визуальные снапшоты сравниваются в автоматическом режиме, порог diff ≤ 2%.

## 8. Интеграция фронтенд модулей
- `modules/world` отвечает за World Pulse, Events Dashboard, Influence Map.
- `modules/economy` предоставляет Market Stabilizer и интеграцию с торговыми интерфейсами.
- `modules/social` реализует Guild Operations и Social Resonance панели.
- Общие компоненты: `shared/ui/CyberpunkMap`, `shared/ui/StatPill`, `shared/ui/StatusPulse`.
- State management: `@necp/state/world` slice, синхронизация через RTK Query с `world-service`.
- Offline-support: локальный persist на 15 минут для базовых метрик, отображение баннера «данные устарели».
- Feature toggles: конфигурация через `world-service` (endpoint `/api/v1/world/config/ui`) позволяет включать/выключать новые панели без деплоя.
- Accessibility: поддержка screen-reader, color-blind тем, масштабирование текста до 150% без потери структуры.
- Error handling: единый `WorldErrorBoundary` и fallback-сцены для `calm/alert/crisis` режимов.

## 9. Настройка команд и последующие действия
- Согласовать макеты с UI/UX и обновить `ui-system.md` (ответственный: Front Manager, срок: 2025-11-08).
- Передать таблицу API в `2025-11-07-api-spec-batch-plan.md` для постановки задач (Brain → API Task Creator, срок: 2025-11-08).
- Дополнить `global-state-management.md` SLA по фоновой автоматике (Backend Manager, срок: 2025-11-09).
- QA Lead подготовит сценарии user testing и тест-кейсы offline/кризиса (`QA/world-test-plan.md`, срок: 2025-11-12).

## 10. Открытые вопросы (не блокируют готовность)
1. Нужно ли пользователю временно скрывать World Pulse для уменьшения информационной нагрузки?
2. Требуется ли companion-приложение для гильдейских приказов (roadmap 2026)?
3. Какой формат реплея мировых событий предпочтителен (видео/текст/графика)?

## 11. История изменений
- 2025-11-07 16:53 — Добавлены ASCII-мокапы, расширенный тест-план, сформированы API задачи и обновлены SLA.
- 2025-11-07 16:45 — Финализированы UX-флоу, таблицы API/событий и планы команд, подтверждён статус ready.
- 2025-11-07 16:39 — Создан черновик.

## 12. ASCII мокапы ключевых интерфейсов

### 12.1 World Pulse Widget
```
┌──────────────────────────────┐
│ WORLD PULSE       [Calm 🌐 ] │
├──────────────────────────────┤
│ Stability: 82%   Modifier: +2│
│ Active Events: 3 (VIEW)      │
│ Trend: ▲ Stable              │
├──────────────────────────────┤
│ [Open Dashboard] [Forecast]  │
└──────────────────────────────┘
```

### 12.2 Events Dashboard Card
```
┌──────────────────────────────────────────────┐
│ EVENT: Blackwall Breach      🏙️ City Center │
├──────────────────────────────────────────────┤
│ State: Active  | Players: 126 | Time: 12:34 │
│ Difficulty: High | Rewards: Legendary Loot  │
├──────────────────────────────────────────────┤
│ Objectives:                                      │
│ 1. Secure data node                              │
│ 2. Defeat corp response                          │
├──────────────────────────────────────────────┤
│ [View History] [Subscribe] [Open Map]          │
└──────────────────────────────────────────────┘
```

### 12.3 Influence Map Overlay
```
┌──────────────────────────────────────────────┐
│ LAYER: CONTROL  | Region: Watson             │
├────┬────┬────┬────┬────┬────┬────┬────┬────┤
│R45 │R46 │R47 │R48 │R49 │R50 │R51 │R52 │…  │
├────┼────┼────┼────┼────┼────┼────┼────┼────┤
│75% │62% │40% │12% │18% │54% │88% │67% │    │
└────┴────┴────┴────┴────┴────┴────┴────┴────┘
Tooltip:
┌─────────────────────────────────┐
│ Territory: Docklands            │
│ Controller: Night Runners (75%) │
│ Bonuses: +10% Supply, -5% Tax   │
│ Pending Event: Corp Raid (T-02h)│
│ [Open Orders] [View Mission]    │
└─────────────────────────────────┘
```

### 12.4 Guild Operations Timeline
```
┌───────────────────────────────┐
│ ORDER: Defend Docklands       │
├────────┬────────┬────────────┤
│ Draft  │ Review │ Active     │
│(Leader)│(Strateg│(Live Ops)  │
├────────┴────────┴────────────┤
│ Resources: 250k ed, 20 squads │
│ KPI: Hold control ≥60%        │
├───────────────────────────────┤
│ [Approve] [Adjust] [Close]    │
└───────────────────────────────┘
```

### 12.5 Market Stabilizer Console
```
┌──────────────────────────────────────────────┐
│ MARKET STABILIZER       Risk: Medium         │
├──────────────┬──────────────────────────────┤
│ Price Watch   │ Interventions                │
│ Cyberware 18k │ ● Freeze Prices (24h)        │
│ Components 5k │ ○ Strategic Reserve (48h)    │
│ Medicine 9k   │ ○ Tax Incentive (72h)        │
├──────────────┴──────────────────────────────┤
│ Est. Impact: -12% volatility                 │
│ Cost: 120k ed | Cooldown: 8h                 │
│ [Simulate] [Confirm (MFA)]                   │
└──────────────────────────────────────────────┘
```

### 12.6 Social Resonance Panel
```
┌──────────────────────────────────────────────┐
│ SOCIAL RESONANCE        Trust Index: 64 ⭐   │
├──────────────┬──────────────────────────────┤
│ Campaigns    │ Effects                       │
│ ● HarmonyFest│ +5 Trust (Economy)            │
│ ○ Night Patrol│ +3 Trust (Security)          │
│ ○ Media Blitz │ +4 Trust (Reputation)        │
├──────────────┴──────────────────────────────┤
│ Relationships:                               │
│ - NetRunner99 (Romantic T2) +2 Trust         │
│ - NeonGuild (Alliance T1) -1 Trust           │
│ [Launch] [Review Network]                    │
└──────────────────────────────────────────────┘
```

### 12.7 Crisis Control Hub
```
┌──────────────────────────────────────────────┐
│ CRISIS HUB – Phase: RESPONSE  ETA 00:12:45   │
├──────────────┬──────────────┬───────────────┤
│ Evacuation   │ Militarization│ Economy       │
│ ● Deploy TT  │ ○ Drone Patrol │ ● Price Freeze│
│ ○ Safe Tunnels│ ● Blockades   │ ○ Subsidies   │
├──────────────┴──────────────┴───────────────┤
│ Active: Deploy TT (Cooldown 05:32)           │
│ Effect: Price Freeze +15% Stability          │
├──────────────────────────────────────────────┤
│ [Trigger Action] [View Logs] [Escalate]      │
└──────────────────────────────────────────────┘
```

## 13. Тест-план (manual + automated)

### 13.1 Manual сценарии
- **MP-01 World Pulse Calm:** загрузка HUD при стабильном мире, проверка отображения модификатора, активных событий, переходов по CTA.
- **MP-02 World Pulse Crisis:** эмуляция `alertLevel=crisis`, проверка появления баннера, переход в Crisis Hub, возврат в HUD.
- **ED-01 Фильтры событий:** комбинация фильтров по региону/фракции, проверка серверной пагинации, отсутствие дубликатов.
- **ED-02 История влияния:** открытие карточки события, сверка данных с `world/history`, проверка подписки на уведомления.
- **IM-01 Слои Influence Map:** переключение между `control/population/economy/security/events`, проверка легенд и подсказок.
- **GO-01 Workflow приказа:** создание → ревью → активация → завершение, проверка уведомлений и таймлайна.
- **MS-01 Интервенция рынка:** симуляция интервенции с успешным завершением, проверка обновления World Pulse.
- **SR-01 Кампания доверия:** запуск кампании, проверка изменения индекса и отображения в World Pulse.
- **CH-01 Кризисные действия:** запуск эвакуации и экономической меры, проверка таймеров и логов.
- **OFF-01 Offline fallback:** отключение WebSocket, проверка сообщений деградации и REST-refetch.

### 13.2 Automated (e2e/UI)
- **AUT-01 World Pulse Snapshot:** Cypress/Playwright проверяет наличие ключевых элементов в режимах `calm` и `alert`.
- **AUT-02 Events Pagination:** e2e-скрипт прогоняет фильтры, подтверждая корректность списка и отсутствие пропусков.
- **AUT-03 Influence Map Tiles:** проверка загрузки тайлов при зумировании, содержимого tooltip.
- **AUT-04 Guild Orders Flow:** эмуляция ролей, проверка переходов статусов и уведомлений.
- **AUT-05 Market Intervention Polling:** смена статусов `pending→executing→completed`.
- **AUT-06 Crisis Hub Actions:** запуск трёх действий, проверка `CrisisActionTimeline`.
- **AUT-07 Offline Mode:** проверка локального persist и баннера «данные устарели».

### 13.3 Automated (backend API)
- **API-01 World State Consistency:** контрактный тест `GET /api/v1/world/state` против схемы.
- **API-02 Event Update Stream:** проверка `WORLD_EVENT_UPDATE` и payload.
- **API-03 Territory Metrics:** `GET /territories/{tileId}` с контрольными значениями.
- **API-04 Orders Idempotency:** повторный `POST /world/orders` с одинаковым idempotency key.
- **API-05 Market Intervention Rollback:** автооткат при истечении тайм-аута `executing`.
- **API-06 Social Index Nightly:** Cron тест пересчёта trustIndex и события `SOCIAL_INDEX_CHANGED`.
- **API-07 Crisis Actions Authorization:** проверка RBAC и двойного подтверждения.

### 13.4 Регрессия и нагрузка
- **REG-01 Smoke Suite:** запуск основного набора после каждого деплоя world-service и client.
- **REG-02 Crisis Regression:** еженедельный регресс Crisis Hub (manual + auto).
- **LOAD-01 Events Storm:** нагрузочный тест 5000 одновременных игроков, latency <200 ms.
- **LOAD-02 Map Streaming:** 200 req/s по тайлам, CPU <70%, latency <150 ms.
- **RESILIENCE-01 Kafka Lag:** lag 10 сек, проверка PagerDuty P1 и отображения деградации.
- **CHAOS-01 Service Kill:** отключение world-service instance, восстановление ≤60 сек.

## 14. API задачи и мэппинг
- **API-TASK-241** `api/v1/world/world-interaction-suite.yaml` — World Pulse, Events Dashboard, Influence Map, Guild Ops, Crisis; статус `queued`.
- **API-TASK-242** `api/v1/economy/market/stabilizer.yaml` — Market Stabilizer интервенции и аналитика; статус `queued`.
- **API-TASK-243** `api/v1/social/resonance.yaml` — Social Resonance, кампании доверия и связи; статус `queued`.
- Обновлены записи в `readiness-tracker.yaml` и `API-SWAGGER/tasks/config/brain-mapping.yaml`.

## 15. Аналитика и telemetry (требования)
- **World Pulse:** метрики `world.stability.index`, `world.modifier`, `world.alert.level`, время реакции UI (custom event `world_pulse_click`).
- **Events Dashboard:** события `event_filter_applied` (фильтры), `event_subscribed`, `event_history_opened`; метрики latency загрузки и количество подписок.
- **Influence Map:** heatmap interactions `territory_hover`, `territory_open_orders`, `layer_switch`; измеряем время загрузки тайлов, количество CTA "отдать приказ".
- **Guild Operations:** `order_created`, `order_approved`, `order_failed`, `order_completed`, `order_kpi_recorded`; логируем задержку между стадиями.
- **Market Stabilizer:** `intervention_simulated`, `intervention_submitted`, `intervention_mfa_failed`, `intervention_completed`; отслеживаем `risk_index` и `volatility_delta`.
- **Social Resonance:** `campaign_launched`, `campaign_cancelled`, `relationship_updated`; метрика `trust_index_delta`, `mood_state`.
- **Crisis Hub:** `crisis_action_triggered`, `crisis_action_failed`, `crisis_phase_changed`; latency выполнения, эффективность (`mitigation_level_delta`).
- **Telemetry pipeline:** события отправляются в Kafka topics `analytics.world-ui.*`, агрегируются в ClickHouse; дашборды Grafana: World Live Ops, Economy Interventions, Social Resonance.
- **Алёрты:** автоматические предупреждения при SLA нарушениях (см. секцию 7), уведомление команд через PagerDuty/Slack.

